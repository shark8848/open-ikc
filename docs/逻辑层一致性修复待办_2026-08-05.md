# 逻辑层一致性修复待办（2026-08-05）

> 来源：`docs/API接口定义一致性核查报告_2026-08-05.md`（只读审计，20 项发现）。
> 本文档为**逻辑层实现收尾的交接清单**；文档同步项（B-07/A-02/B-03~05/B-06/错误码表）已由 SDK 侧先行修正，无需重复处理。
> 完成标准：每项闭环后跑 `pytest tests -q`（沙箱外），并同步更新 README §当前实现进度 与 worklog。

## P0 行为正确性（推荐优先）

| # | 位置 | 问题 | 建议动作 | 验收 |
| --- | --- | --- | --- | --- |
| 1 | `app/services/document.py:191`（ingest quick 分支） | `record.status=PARSING` 但响应硬编码 `taskStatus=INGESTED`，与 schema/README 语义不符 | 返回 `record.status`（quick→PARSING） | 新测试覆盖 quick 分支返回 PARSING |
| 2 | `app/services/parse.py:80` + `app/routers/parse.py:49` | 请求体 `kbId` 声明必填但 service 全程未引用，未与 `doc.kb_id` 比对，可越库解析 | 解析时校验 `kbId == doc.kb_id`，不符返回 `100001/100404` | 新测试覆盖 kbId 不匹配 |
| 3 | `app/services/document.py:243`（ingest_and_parse sync 分支） | `executeMode=sync` 时 `resultInline` 恒 `{}`、`taskStatus=PARSING`，静默空成功 | 按 B-06 语义返回内联结果，或明确标注占位并返回 `501001` | sync 分支行为明确且测试覆盖 |

## P1 契约/范式对齐

| # | 位置 | 问题 | 建议动作 |
| --- | --- | --- | --- |
| 4 | `app/schemas/parse.py:71` | `DocumentParseResponse.data` 为裸 dict，违背「data 为明确字段模型」范式 | 新增 `DocumentParseData(taskId/taskStatus/executeMode/resultInline)` 并引用 |
| 5 | `app/routers/search.py:28-30` | `POST /query` 缺 summary/description/response_model；payload 可空被静默转空对象 | 补端点三件套 + `SearchQueryResponse(data: SearchQueryData)`；payload 改必填（落地检索时） |
| 6 | `app/core/authz/runtime.py:59,83` | 内置角色映射无 parse 域权限（`km_reader` 无 `parse:read` 等） | 映射补 `parse:read`/`parse:parse`，或统一归入 document 域 |
| 7 | `app/core/exception_handlers.py:20` | 参数校验错误 `data={"detail": exc.errors()}` 为 `dict[str,list]`，与错误响应 `dict[str,str]` 范式不符（全域影响） | 统一错误 data 形状（结构化为 field/reason 或明确放开约束） |
| 8 | `app/services/parse.py:223` + `app/schemas/parse.py:96` | 下载占位返回 `000000`（note 说明）与「未实现必须 501001」张力 | 契约明确豁免（README 已标注）或改 `501001`；二选一后同步文档 |
| 9 | `app/core/error_codes.py` | `200001/200002/200010` 注册但零使用（残留） | 实际使用 `as_exception(...)` 或标注「预留」；明确 2000xx 按域分段规则 |

## P2 测试与文档补强

| # | 位置 | 问题 | 建议动作 |
| --- | --- | --- | --- |
| 10 | `tests/test_parse.py` | 无参数校验（100001）用例 | 补 kbId/docId 缺失、executeMode 非法用例 |
| 11 | `tests/test_document.py` | 缺 `GET /{doc_id}` 403/401、quick/sync 分支用例 | 按 KB 测试范式补齐 |
| 12 | `tests/`（检索） | 检索路由行为无端到端覆盖 | 补「带鉴权调检索返回 501001」「路由注入 kb_ids 上下文」用例 |
| 13 | `app/services/document.py`（ingest） | directory/archive 来源仅登记单文档伪展开 | 标注占位说明，或实现展开语义后再宣称「真实落地」 |
| 14 | `app/core/responses.py` | 无 document 域响应函数（service 内联，注释「待迁移」） | 收敛到 responses.py，对齐 KB create/update 范式 |
| 15 | `app/routers/search.py:32-44` | 未用 `_request_identity`，ownerId/orgPath 直接取载荷 | 落地检索时从 `request.state.identity` 派生身份注入 context |
| 16 | `app/schemas/parse.py:8` | 请求模型无 model_validator 条件校验（chunkSize 语义仅描述） | 补条件校验或明确为透传 |
| 17 | V2 D-01 字段对齐 | 代码 `query/kbId/kbIds/ownerId/orgPath` vs D-01 `kbId(必填)/teamId/orgId/mode/topK/filters/withCitation` | 落地检索前双向对齐（文档补 ownerId/orgPath，代码补 D-01 字段） |

> 注：#4/#8 与 SDK 解析域封装相关（SDK `ParseTask` 已按目标态建模，契约变更需同步 `docs/开放平台SDK集成设计.md`）。
